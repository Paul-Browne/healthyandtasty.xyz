<main class="home">
    {{#pages}}
    <article>
        <h2>
            <a href="{{link}}">
                {{name}}
                <img src="/images/{{image}}" alt={{name}}>
            </a>
        </h2>
    </article>
    {{/pages}}
</main>